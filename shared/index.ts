export { 
  generateCommitTitle,
  categorizeFiles,
  generateFallbackTitle
} from "./commit-title-generator.ts";

export type {
  SessionMetadata,
  GitCommitInfo,
  ClaudeOutput,
  GitFileChange
} from "./types.ts";